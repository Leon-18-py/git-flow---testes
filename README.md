# git-flow---testes
git clone, clona o repositório
git push, enviar o commit para o repositório
git switch, troca de branch
git branch nome-da-branch,criar branch
git commit -m, criar commit
git commit --amend -m, conserto/correção de commit
